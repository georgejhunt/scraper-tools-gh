###Ideas
* Connect to test machine via wifi, and unplug ethernet wire
* Use dreamweaver to remake index.html at root
* create an env script to define shortcuts
* put the git repo in the space that is served by nginx, and then
* Have the test directory directly beneath
